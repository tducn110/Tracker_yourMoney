# Skeleton Dockerfile cho Worker (Chuẩn bị cho việc triển khai độc lập)
FROM node:20-slim AS base
WORKDIR /app

# Placeholder: Khi nào cần build worker thật, mới copy code vào
CMD ["node", "-e", "console.log('Worker container placeholder')"]
